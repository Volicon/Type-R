{
    "private": true,
    "workspaces" : [
        "models", "ext-types", "globals", "endpoints", "tests", "docs"
    ],

    "scripts" : {
        "build" : "yarn workspaces run build",
        "test" : "yarn workspaces run test"
    }
}