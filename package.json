{
    "private": true,
    "workspaces" : [
        "models", "globals", "ext-types", "endpoints", "docs"
    ],

    "scripts" : {
        "build" : "yarn workspace run build",
        "test" : "yarn workspace run test"
    }
}