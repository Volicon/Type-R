{
    "private": true,
    "workspaces" : [
        "models", "globals", "ext-types", "endpoints", "docs"
    ],

    "scripts" : {
        "build" : "yarn workspaces run build",
        "test" : "yarn workspaces run test"
    }
}