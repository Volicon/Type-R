{
  "name": "@type-r/react",
  "version": "4.0.0",
  "description": "React bindings for Type-R/models",
  "main": "dist/index.js",
  "author": "Vlad Balin",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "react": "^16.8.6"
  },
  "devDependencies": {
    "@types/react": "^16.8.23"
  },

  "scripts": {
    "cleanup": "rimraf ./lib",
    "compile": "yarn cleanup && tsc",
    "bundle": "rollup --config",
    "build": "yarn compile && yarn bundle"
  }
}
